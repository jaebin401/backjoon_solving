{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",  // ← build 디렉토리
            "args": [
                "Hello",
                "World"
            ],
            "cwd": "${workspaceFolder}",     // ← 실행 경로도 루트
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ]
}